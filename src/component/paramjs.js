
const Add = (a, b) => a + b;



/**
 * 
 * @param {*} a => valeur tsy maintsy complet zat vo afaka mi executer le function
 * @param {*} b => valeur tsy maintsy complet zat vo afaka mi executer le function
 * @returns 
 */
function Add(a, b) {
    return a + b;
}

Add(1, 2);